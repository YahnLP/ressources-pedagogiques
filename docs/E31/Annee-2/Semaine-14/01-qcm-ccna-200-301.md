# 📋 QCM STYLE CCNA 200-301 — S14 · 2ᵉ ANNÉE · E31
## 30 Questions d'Entraînement — Conditions d'Examen

---

> **Nom** : ___________________________
> **Date** : ___________________________
> **Durée** : 50 minutes · **Aucune fiche autorisée** · **Calculatrice autorisée**
> **Barème** : 1 point par bonne réponse · 0 point si faux ou non répondu (pas de malus)
> **Score cible E31** : ≥ 20/30 · **Score cible CCNA** : ≥ 25/30

---

## 📐 Domaine 1 — Network Fundamentals (Q1 à Q8)

**Q1** — Un administrateur observe `192.168.10.0/28` dans une table de routage.
Combien d'adresses d'hôtes utilisables ce sous-réseau fournit-il ?

```
A) 8
B) 14  ←
C) 16
D) 30
```

Réponse : ___

---

**Q2** — Quelle couche du modèle OSI est responsable du routage des paquets entre réseaux ?

```
A) Couche 2 — Liaison de données
B) Couche 3 — Réseau  ←
C) Couche 4 — Transport
D) Couche 5 — Session
```

Réponse : ___

---

**Q3** — Une trame Ethernet contient les champs suivants dans son en-tête. Quel champ permet au switch de prendre sa décision de commutation ?

```
A) IP source
B) MAC destination  ←
C) Port TCP source
D) VLAN tag
```

Réponse : ___

---

**Q4** — Quelle est la plage d'adresses IP de classe C privée définie par la RFC 1918 ?

```
A) 10.0.0.0 – 10.255.255.255
B) 172.16.0.0 – 172.31.255.255
C) 192.168.0.0 – 192.168.255.255  ←
D) 169.254.0.0 – 169.254.255.255
```

Réponse : ___

---

**Q5** — Quel est le rôle du protocole ARP (Address Resolution Protocol) ?

```
A) Résoudre un nom de domaine en adresse IP
B) Résoudre une adresse IP en adresse MAC  ←
C) Assigner une adresse IP à un client dynamiquement
D) Vérifier la connectivité réseau entre deux hôtes
```

Réponse : ___

---

**Q6** — Un paquet arrive sur un routeur. La table de routage contient ces entrées :
```
  192.168.10.0/24  via 10.0.1.1
  192.168.0.0/16   via 10.0.2.1
  0.0.0.0/0        via 10.0.3.1
```
La destination est `192.168.10.55`. Quelle route est utilisée ?

```
A) 192.168.0.0/16 via 10.0.2.1
B) 192.168.10.0/24 via 10.0.1.1  ←  (plus spécifique = plus long préfixe)
C) 0.0.0.0/0 via 10.0.3.1
D) Le paquet est droppé
```

Réponse : ___

---

**Q7** — Quel protocole utilise le port UDP 67 et 68 ?

```
A) DNS
B) TFTP
C) DHCP  ←
D) SNMP
```

Réponse : ___

---

**Q8** — Quelle est la différence entre un hub et un switch ?

```
A) Un hub filtre les trames par adresse MAC ; un switch diffuse tout
B) Un hub opère en couche 3 ; un switch en couche 2
C) Un hub diffuse tout ; un switch filtre par adresse MAC  ←
D) Il n'y a aucune différence fonctionnelle
```

Réponse : ___

---

## 🔌 Domaine 2 — Network Access (Q9 à Q16)

**Q9** — Quel est le mode de port switch correct pour permettre à plusieurs VLANs de traverser le lien entre deux switches ?

```
A) switchport mode access
B) switchport mode trunk  ←
C) switchport mode dynamic
D) switchport nonegotiate
```

Réponse : ___

---

**Q10** — Un switch reçoit une trame avec un tag VLAN 30. Le port de sortie est configuré en `access vlan 10`. Que fait le switch ?

```
A) Il transmet la trame sur le port
B) Il supprime le tag et transmet dans VLAN 10
C) Il droppe la trame  ←  (VLAN 30 ≠ VLAN 10 du port access)
D) Il retransmet la trame sur tous les ports
```

Réponse : ___

---

**Q11** — Quelle est la valeur de la distance administrative d'une route OSPF ?

```
A) 1
B) 90
C) 110  ←
D) 120
```

Réponse : ___

---

**Q12** — Dans une sortie `show etherchannel summary`, un port affiche le code `(I)`.
Que cela signifie-t-il ?

```
A) Interface down (câble débranché)
B) Interface stand-alone — elle n'a pas rejoint le bundle  ←
C) Interface en mode half-duplex
D) Interface configurée en LACP passive uniquement
```

Réponse : ___

---

**Q13** — Quel protocole d'agrégation de liens est défini par le standard IEEE 802.3ad ?

```
A) PAgP
B) LACP  ←
C) STP
D) RSTP
```

Réponse : ___

---

**Q14** — Un switch Cisco a le port Gi0/1 en `spanning-tree portfast`. Que signifie portfast ?

```
A) Le port passe directement à l'état Forwarding sans attendre la convergence STP  ←
B) Le port utilise une version accélérée de RSTP
C) Le port est configuré pour transmettre plus vite en mode full-duplex
D) Le port est désactivé dans la topologie STP
```

Réponse : ___

---

**Q15** — Quelle commande affiche l'état de tous les ports d'un switch, y compris leur VLAN et leur statut ?

```
A) show interfaces
B) show vlan brief  ←
C) show spanning-tree
D) show ip route
```

Réponse : ___

---

**Q16** — Dans quel mode LACP les deux switches initient-ils la négociation EtherChannel ?

```
A) passive / passive
B) on / on
C) active / passive
D) active / active  ←  (les deux envoient des LACPDU)
```

Réponse : ___

---

## 🌐 Domaine 3 — IP Connectivity (Q17 à Q22)

**Q17** — Quelle commande OSPF indique à un routeur Cisco d'annoncer le réseau `10.0.0.0/8` dans l'Area 0 ?

```
A) network 10.0.0.0 0.0.0.0 area 0
B) network 10.0.0.0 0.255.255.255 area 0  ←  (wildcard = inverse du masque)
C) network 10.0.0.0 255.0.0.0 area 0
D) network 10.0.0.0 area 0
```

Réponse : ___

---

**Q18** — Un routeur affiche `O IA 172.16.5.0/24 [110/30] via 10.0.0.1`.
Que signifie `O IA` ?

```
A) Route apprise via OSPF depuis la même aire  
B) Route apprise via OSPF depuis une aire différente  ←  (Inter-Area)
C) Route externe redistribuée dans OSPF
D) Route OSPF vers une interface Loopback
```

Réponse : ___

---

**Q19** — Quelle est la formule du coût OSPF par défaut pour un lien FastEthernet 100 Mbps ?

```
A) Coût = 100 / 100 = 1  ←
B) Coût = 1 000 / 100 = 10
C) Coût = 10 / 100 = 0,1
D) Coût = 100 × 100 = 10 000
```

Réponse : ___

---

**Q20** — Un routeur a la table de routage suivante :
```
C    192.168.1.0/24 is directly connected, Gi0/0
S    192.168.2.0/24 [1/0] via 10.0.0.2
O    192.168.3.0/24 [110/2] via 10.0.0.1
S*   0.0.0.0/0 [1/0] via 10.0.0.2
```
Quelle route est utilisée pour un paquet vers `192.168.3.15` ?

```
A) La route statique par défaut
B) La route statique vers 192.168.2.0/24
C) La route OSPF [110/2] via 10.0.0.1  ←
D) La route directement connectée
```

Réponse : ___

---

**Q21** — Quel est l'avantage principal d'OSPF par rapport au routage statique ?

```
A) OSPF est plus facile à configurer
B) OSPF converge automatiquement en cas de panne de lien  ←
C) OSPF utilise moins de bande passante
D) OSPF ne nécessite pas de configuration d'adresse IP
```

Réponse : ___

---

**Q22** — Une route `S* 0.0.0.0/0` est présente dans la table. Pour quel type de paquets sera-t-elle utilisée ?

```
A) Tous les paquets sans exception
B) Uniquement les paquets vers Internet
C) Les paquets pour lesquels aucune route plus spécifique n'existe  ←
D) Les paquets dont la TTL est à 0
```

Réponse : ___

---

## 🛡️ Domaines 4 & 5 — IP Services + Security (Q23 à Q28)

**Q23** — Quelle commande active DHCP Snooping sur un switch Cisco pour le VLAN 10 ?

```
A) ip dhcp snooping vlan 10  ←
B) dhcp snooping enable vlan 10
C) switchport dhcp-snooping vlan 10
D) ip dhcp snooping trust
```

Réponse : ___

---

**Q24** — Un port configuré avec `switchport port-security maximum 1` et `violation shutdown`.
Que se passe-t-il si une deuxième adresse MAC est détectée ?

```
A) La nouvelle adresse MAC est enregistrée
B) Le port passe en mode err-disabled  ←
C) Les deux adresses MAC sont autorisées
D) Le port envoie un trap SNMP uniquement
```

Réponse : ___

---

**Q25** — Quel protocole est utilisé pour l'authentification dans un réseau WiFi entreprise 802.1X ?

```
A) WPA2-PSK
B) RADIUS  ←
C) TACACS
D) LDAP
```

Réponse : ___

---

**Q26** — Une ACL standard est appliquée `in` sur l'interface Gi0/0.
`access-list 10 deny host 192.168.1.100`
`access-list 10 permit any`
Quel trafic est affecté ?

```
A) Tout le trafic vers 192.168.1.100 est bloqué
B) Tout le trafic provenant de 192.168.1.100 est bloqué  ←
C) Tout le trafic est bloqué (deny implicite en fin)
D) Rien n'est bloqué (permit any en fin autorise tout)
```

Réponse : ___

---

**Q27** — Quelle technique QoS permet de garantir un débit minimum à une classe de trafic tout en permettant aux autres classes d'utiliser la bande passante disponible ?

```
A) FIFO (First In First Out)
B) Priority Queue (strict priority)
C) CBWFQ (Class-Based Weighted Fair Queuing)  ←
D) Traffic Shaping
```

Réponse : ___

---

**Q28** — Quelle valeur DSCP est associée au trafic VoIP (Expedited Forwarding) ?

```
A) DSCP 0 (Best Effort)
B) DSCP 26 (AF31)
C) DSCP 46 (EF)  ←
D) DSCP 48 (CS6)
```

Réponse : ___

---

## 🤖 Domaine 6 — Automation (Q29 à Q30)

**Q29** — Quel format de données est le plus couramment utilisé pour les API REST dans les réseaux programmables ?

```
A) XML
B) CSV
C) JSON  ←
D) YAML
```

Réponse : ___

---

**Q30** — Quelle est la différence entre un réseau défini par logiciel (SDN) et un réseau traditionnel ?

```
A) SDN est plus lent car le plan de contrôle est centralisé
B) Dans SDN, le plan de contrôle est séparé du plan de données et centralisé  ←
C) SDN remplace les switches par des routeurs
D) SDN nécessite l'utilisation exclusive du protocole OSPF
```

Réponse : ___

---

## 📊 Grille de score et analyse

| Domaine | Questions | Score /8 |
|---|---|---|
| Fundamentals | Q1-Q8 | /8 |
| Network Access | Q9-Q16 | /8 |
| IP Connectivity | Q17-Q22 | /6 |
| IP Services + Sécurité | Q23-Q28 | /6 |
| Automation | Q29-Q30 | /2 |
| **TOTAL** | | **/30** |

**Interprétation :**
- 25-30 : Excellent — niveau certification CCNA accessible
- 20-24 : Bon — quelques points à consolider avant E31
- 15-19 : Moyen — révisions ciblées nécessaires (identifier les domaines faibles)
- < 15 : Révisions intensives requises — reprendre les séances S2, S3, S13

**Mes 3 domaines les plus faibles :**
```
1. _____________________________________ → revoir séance ______
2. _____________________________________ → revoir séance ______
3. _____________________________________ → revoir séance ______
```

---

---

# ✅ CORRECTION QCM — Document enseignant uniquement

| Q | Rép. | Justification clé |
|---|---|---|
| 1 | B | /28 → 4 bits hôte → 2⁴-2 = 14 hôtes |
| 2 | B | Routage = couche 3 OSI (IP) |
| 3 | B | Switch décide sur MAC destination (CAM table) |
| 4 | C | RFC 1918 : 192.168.0.0/16 |
| 5 | B | ARP = IP → MAC (broadcast L2) |
| 6 | B | Longest prefix match : /24 > /16 > /0 |
| 7 | C | DHCP : serveur écoute port 67, client 68, UDP |
| 8 | C | Hub = broadcast domain · Switch = collision domain isolé par port |
| 9 | B | Trunk = multi-VLAN sur un seul lien physique |
| 10 | C | Port access VLAN 10 : trame taggée VLAN 30 = droppe |
| 11 | C | DA OSPF = 110 (statique=1, OSPF=110, RIP=120) |
| 12 | B | (I) = stand-alone, interface UP mais pas dans le bundle |
| 13 | B | 802.3ad = LACP (PAgP = Cisco propriétaire) |
| 14 | A | Portfast bypass les états STP (Listening, Learning) |
| 15 | B | `show vlan brief` = VLAN + ports associés + statut |
| 16 | D | active/active = les deux envoient des LACPDU |
| 17 | B | Wildcard mask = inverse du masque : 0.255.255.255 pour /8 |
| 18 | B | O IA = OSPF Inter-Area (autre aire, via ABR) |
| 19 | A | Coût FastEthernet = 100Mbps/100Mbps = 1 |
| 20 | C | O [110/2] : route OSPF plus spécifique que la default |
| 21 | B | Convergence automatique = avantage principal des protocoles dynamiques |
| 22 | C | Route par défaut = attracteur pour tout trafic sans correspondance |
| 23 | A | `ip dhcp snooping vlan 10` |
| 24 | B | Violation shutdown → err-disabled |
| 25 | B | RADIUS = protocole d'authentification AAA pour 802.1X |
| 26 | B | ACL standard filtre sur IP SOURCE uniquement |
| 27 | C | CBWFQ = bande passante garantie par classe |
| 28 | C | DSCP 46 = EF = Expedited Forwarding = VoIP |
| 29 | C | JSON = format API REST standard dans les réseaux modernes |
| 30 | B | SDN = plan de contrôle centralisé séparé du plan de données |

---

*QCM CCNA 200-301 — BAC PRO CIEL | E31 | 2ᵉ année S14*
*Bilan compétences : C2.2 · C2.3 · S2.1 · S2.2 · S2.3 · S3.3*
