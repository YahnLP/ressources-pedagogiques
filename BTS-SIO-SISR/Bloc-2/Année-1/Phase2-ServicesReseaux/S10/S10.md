# Pack de Formation - Semaine 10 (S10) - BLOC 2
## "Agent Relais DHCP, TP Intégré et Bilan Phase 2"

---

## 📋 FICHE ENSEIGNANT

### Informations Générales

| **Élément** | **Détails** |
|-------------|-------------|
| **Semaine** | S10 - Année 1 |
| **Bloc** | Bloc 2 - Infrastructure, Systèmes & Réseaux |
| **Durée** | 4 heures (1 séance) |
| **Phase** | Phase 2 - **Bilan et évaluation** |
| **Public** | Apprentis BTS SIO SISR (hétérogène) |
| **Prérequis** | **TOUT S6–S9** : Switch/STP, CLI Cisco, VLANs, routage statique, DHCP Linux, DNS Bind9 |

### Compétences RNCP Visées

| **Code** | **Libellé** | **Niveau** |
|----------|-------------|------------|
| **B2.2** | Installer, tester et déployer une infrastructure réseau complète (VLANs + routage + DHCP + DNS) | **Maîtrise** |
| **B2.3** | Exploiter, dépanner et superviser — diagnostic complet multi-services | Maîtrise |
| **B2.1** | Administrer les services d'un système d'exploitation serveur — DHCP + DNS intégrés | Maîtrise |

### Objectifs Pédagogiques

**À l'issue de cette séance, l'apprenant sera capable de :**

1. **Expliquer** pourquoi un serveur DHCP unique ne peut pas servir plusieurs VLANs sans agent relais
2. **Configurer** `ip helper-address` sur une interface de routeur pour relayer les requêtes DHCP
3. **Déployer** une infrastructure réseau intégrant VLANs, routage inter-VLAN, DHCP Linux, DNS Bind9
4. **Mettre en relation** tous les services configurés (switch → routeur → serveur Linux)
5. **Diagnostiquer** méthodiquement une panne dans une infrastructure multi-services
6. **Démontrer** sa maîtrise de la Phase 2 lors du TP noté évaluatif

---

### Planning de Séance (4h)

| **Horaire** | **Durée** | **Activité** | **Modalité** | **Objectif** |
|-------------|-----------|--------------|--------------|--------------|
| 00:00 – 00:15 | 15 min | **Révisions rapides** : quiz oral Phase 2 (S6 → S9), correction exercices | Collectif | Réactiver tous les acquis |
| 00:15 – 00:45 | 30 min | Apport théorique : Agent relais DHCP (`ip helper-address`), problème DHCP inter-VLAN | Collectif + démo Packet Tracer | Nouveau concept |
| 00:45 – 01:45 | 60 min | **TP Intégré guidé** : Infrastructure complète 2 VLANs + routage + DHCP + DNS | Individuel encadré | Synthèse |
| 01:45 – 02:00 | 15 min | **Pause** + distribution sujet évaluation | – | – |
| 02:00 – 04:00 | 120 min | **ÉVALUATION FORMATIVE – TP NOTÉ** (sujet indépendant) | **Individuel, en silence** | Évaluer |

> **Note organisationnelle :** Le TP intégré guidé (première partie) est une répétition générale. L'évaluation (deuxième partie) porte sur un sujet différent mais utilise les mêmes compétences. Les apprenants peuvent garder leurs notes de cours.

---

### Matériel Nécessaire

| **Type** | **Quantité** | **Description** |
|----------|--------------|-----------------|
| PC Windows 10/11 | 1 par apprenant | Packet Tracer + VM Debian |
| Packet Tracer 8.x | Installé | Switch 2960 + Routeur 1841/2911 disponibles |
| VM Debian 12 | 1 par apprenant | Bind9 + isc-dhcp-server installés (S8/S9) |
| Fiche aide-mémoire Phase 2 | 1 par apprenant | Toutes les commandes clés S6–S9 (autorisée pendant l'éval) |
| Sujet évaluation imprimé | 1 par apprenant | Distribué à la pause |
| Grille d'évaluation | 1 par enseignant | Pour la correction |

**Préparation avant séance :**
- ✅ Préparer et imprimer le sujet de TP noté (section Évaluation)
- ✅ Préparer la fiche aide-mémoire récapitulative Phase 2 (fournie ci-dessous)
- ✅ Vérifier que les VM Debian ont Bind9 ET isc-dhcp-server installés depuis S8/S9
- ✅ Préparer un fichier `.pkt` de départ pour le TP intégré (topologie pré-câblée sans config)

---

### Conseils de Différenciation

#### Apprenants **Bac Pro CIEL**

- **Agent relais avancé** : Configurer l'`ip helper-address` pour relayer vers **deux** serveurs DHCP (redondance). Analyser les trames DHCP relayées avec Wireshark (le champ `giaddr` = Gateway IP Address contient l'IP de l'interface relais).
- **TP intégré avancé** : Ajouter un troisième VLAN (VLAN Serveurs, 192.168.30.0/24) avec son étendue DHCP et ses enregistrements DNS. Configurer le routage entre les 3 VLANs.
- **Évaluation** : Si terminé en avance, ajouter un enregistrement DNS MX et/ou configurer l'accès SSH sur le routeur.

#### Apprenants **débutants**

- **Phase de révision** : Distribuer la fiche aide-mémoire 10 minutes avant la fin de la pause pour qu'ils puissent la parcourir.
- **TP intégré** : Décomposer en étapes très courtes avec validation enseignant à chaque jalon. Fournir un tableau de suivi de progression.
- **Évaluation** : Rappeler que la fiche aide-mémoire est autorisée. Tolérance sur l'ordre des configurations si le résultat final est cohérent.

---

### Points de Vigilance

⚠️ **`ip helper-address` : interface d'application**
La commande `ip helper-address` se configure sur l'**interface du routeur qui reçoit les requêtes DHCP broadcast** (côté client), **pas** sur l'interface côté serveur. Erreur fréquente : la configurer dans le mauvais sens.

⚠️ **`ip helper-address` + routage inter-VLAN**
Pour que le relay fonctionne, l'interface sub-interface du routeur (`interface Fa0/0.10`) doit avoir une IP dans le VLAN source **ET** le routeur doit avoir une route vers le serveur DHCP. Si le serveur DHCP est dans un autre VLAN, vérifier que la route est en place.

⚠️ **Cohérence DHCP et DNS**
Le serveur DHCP envoie aux clients l'IP du serveur DNS dans `option domain-name-servers`. Cette IP doit être l'IP **réelle** du serveur Bind9, atteignable depuis le VLAN client. Erreur fréquente : mettre `127.0.0.1` dans l'option DNS du serveur DHCP.

⚠️ **Évaluation : gestion du temps**
120 minutes = environ 20-25 minutes par grande section. Rappeler à 60 min et à 30 min restants. Les apprenants ont tendance à bloquer trop longtemps sur la configuration Cisco avant de passer au Linux.

⚠️ **Packet Tracer : simulation DNS**
Packet Tracer ne simule **pas** complètement le DNS Bind9. Dans le TP intégré, le serveur DNS sera configuré sur la VM Linux (réel), tandis que le routage/DHCP sera sur Packet Tracer. Il faudra donc soit séparer les deux exercices, soit utiliser uniquement Packet Tracer avec le serveur DNS simulé (moins réaliste). **Recommandation :** TP intégré = Packet Tracer pour toute la partie réseau + VM pour DHCP/DNS ; le TP noté = Packet Tracer seul avec DHCP simulé Cisco + DNS simulé.

---

### Fiche Aide-Mémoire Phase 2 (à distribuer aux apprenants)

```
╔══════════════════════════════════════════════════════════════════════╗
║          AIDE-MÉMOIRE PHASE 2 – BTS SIO SISR – Bloc 2              ║
╠══════════════════════════════════════════════════════════════════════╣
║  SWITCH – S6                                                         ║
║  show mac address-table    | Table MAC                               ║
║  show spanning-tree        | État STP                                ║
║  spanning-tree vlan 1 priority 4096  | Forcer root bridge            ║
╠══════════════════════════════════════════════════════════════════════╣
║  VLANS – S7                                                          ║
║  vlan 10 / name VLAN_INFO  | Créer VLAN sur switch                   ║
║  interface Fa0/X           |                                         ║
║    switchport mode access  | Port d'accès                            ║
║    switchport access vlan 10                                         ║
║  interface Fa0/Y           |                                         ║
║    switchport mode trunk   | Port trunk                              ║
║    switchport trunk allowed vlan 10,20                               ║
║  show vlan brief           | Vérifier VLANs                          ║
╠══════════════════════════════════════════════════════════════════════╣
║  ROUTEUR (inter-VLAN) – S8                                           ║
║  interface Fa0/0.10        | Sous-interface VLAN 10                  ║
║    encapsulation dot1Q 10  |                                         ║
║    ip address 192.168.10.1 255.255.255.0                             ║
║    no shutdown             |                                         ║
║  ip route 0.0.0.0 0.0.0.0 [next-hop]  | Route par défaut            ║
║  ip helper-address [IP_serveur_DHCP]  | Agent relais DHCP           ║
║  show ip route / show ip interface brief                             ║
╠══════════════════════════════════════════════════════════════════════╣
║  DHCP LINUX – S8                                                     ║
║  /etc/dhcp/dhcpd.conf                                                ║
║  subnet X.X.X.0 netmask 255.255.255.0 {                             ║
║    range X.X.X.100 X.X.X.200;                                        ║
║    option routers X.X.X.1;                                           ║
║    option domain-name-servers X.X.X.Z;                              ║
║  }                                                                   ║
║  /etc/default/isc-dhcp-server → INTERFACESv4="eth0"                 ║
║  sudo systemctl restart isc-dhcp-server                              ║
║  cat /var/lib/dhcp/dhcpd.leases  | Baux actifs                       ║
╠══════════════════════════════════════════════════════════════════════╣
║  DNS BIND9 – S9                                                      ║
║  /etc/bind/named.conf.local → déclarer zones                         ║
║  /etc/bind/db.NOM_DOMAINE  → zone directe (A, CNAME, NS, SOA)       ║
║  /etc/bind/db.X.Y.Z.rev    → zone inverse (PTR)                      ║
║  sudo named-checkzone NOM /etc/bind/db.NOM  | Vérifier syntaxe       ║
║  sudo systemctl restart bind9                                        ║
║  dig @127.0.0.1 www.domaine.local  | Tester résolution               ║
╠══════════════════════════════════════════════════════════════════════╣
║  AGENT RELAIS DHCP – S10                                             ║
║  Sur l'interface du routeur côté VLAN client :                        ║
║  Router(config-if)# ip helper-address [IP_serveur_DHCP]              ║
╚══════════════════════════════════════════════════════════════════════╝
```

---

### Évaluation Formative (TP Noté) – Vue Enseignant

**Durée :** 120 minutes  
**Documents autorisés :** Fiche aide-mémoire Phase 2 (fournie), notes personnelles  
**Matériaux :** Packet Tracer + VM Debian  
**Barème :** /30 (ramené à /20 pour le livret)

**Grille de notation rapide enseignant :**

| **Section** | **Critère clé** | **Vérification** | **Points** |
|-------------|-----------------|------------------|------------|
| VLANs | VLAN 10 et 20 créés et affectés | `show vlan brief` | /4 |
| Trunk | Port trunk configuré vers routeur | `show int trunk` | /2 |
| Routeur | Sous-interfaces + IPs + no shutdown | `show ip int brief` | /4 |
| Agent relais | `ip helper-address` sur bonnes interfaces | `show running-config` | /3 |
| DHCP Linux | 2 étendues (VLAN 10 et 20) configurées | `systemctl status` | /4 |
| DNS Linux | Zone directe + 3 enregistrements A + 1 CNAME | `dig` réussit | /4 |
| Tests | Ping inter-VLAN, résolution DNS | Captures | /5 |
| Sauvegarde | `copy run start` | `show startup-config` | /2 |
| Rapport | Synthèse écrite (1 page) | Lisibilité, pertinence | /2 |
| **TOTAL** | | | **/30 → /20** |

---

## 📚 COURS : AGENT RELAIS DHCP

### A. Le Problème : DHCP et VLANs

**Rappel :** Le protocole DHCP fonctionne grâce à des messages **broadcast** (`255.255.255.255`). Quand un PC démarre et cherche un serveur DHCP, il crie :

> *"Y a-t-il un serveur DHCP quelque part ? DISCOVER !"*

**Le problème :** Les routeurs **ne transmettent pas les broadcasts** entre réseaux.

Dans une infrastructure avec VLANs :

```
┌──────────────────────────────────────────────────────┐
│ VLAN 10 (192.168.10.0/24)                            │
│   PC-A démarre → envoie DHCP Discover (broadcast)    │
│   Le broadcast reste dans VLAN 10                    │
│   Le serveur DHCP est dans VLAN 30 (192.168.30.0/24) │
│   Le routeur ne transmet PAS le broadcast vers VLAN30 │
│   → PC-A n'obtient jamais de réponse DHCP ❌          │
└──────────────────────────────────────────────────────┘
```

**Le problème :** Faut-il installer un serveur DHCP dans chaque VLAN ? Non — c'est coûteux et difficile à maintenir.

---

### B. La Solution : L'Agent Relais DHCP (`ip helper-address`)

**Principe :** Le routeur intercepte les broadcasts DHCP dans chaque VLAN et les **transmet en unicast** vers le serveur DHCP centralisé.

```
VLAN 10                    ROUTEUR                    VLAN Serveurs
PC-A ──broadcast──→ [Interface Fa0/0.10] → unicast → [Serveur DHCP Linux]
                        ip helper-address             192.168.30.10
                        192.168.30.10
```

**Fonctionnement détaillé :**

```
1. PC-A (VLAN 10) : DHCP Discover (broadcast 255.255.255.255)
       ↓
2. Routeur reçoit sur Fa0/0.10 (VLAN 10)
   → Vérifie si ip helper-address est configuré sur cette interface
   → OUI : transforme le broadcast en unicast vers 192.168.30.10
   → Ajoute son adresse IP (192.168.10.1) dans le champ "giaddr" (Gateway IP)
       ↓
3. Serveur DHCP reçoit la requête relayée en unicast
   → Voit giaddr = 192.168.10.1 → sait que c'est pour la plage du VLAN 10
   → Répond en unicast vers 192.168.10.1 (le routeur)
       ↓
4. Routeur reçoit la réponse DHCP
   → La retransmet vers PC-A (via VLAN 10)
       ↓
5. PC-A obtient une IP dans la plage 192.168.10.0/24
```

**Le champ `giaddr` (Gateway IP Address)** est la clé : il permet au serveur DHCP de savoir dans quel réseau se trouve le client et donc quelle étendue (subnet) utiliser pour lui attribuer une IP.

---

### C. Configuration de l'Agent Relais

#### Côté Routeur Cisco

La commande `ip helper-address` se configure sur chaque **interface (ou sous-interface) côté VLAN client** :

```cisco
! Sur la sous-interface du VLAN 10 (côté clients VLAN 10)
Router(config)# interface FastEthernet0/0.10
Router(config-if)# ip helper-address 192.168.30.10    ! IP du serveur DHCP Linux

! Sur la sous-interface du VLAN 20 (côté clients VLAN 20)
Router(config)# interface FastEthernet0/0.20
Router(config-if)# ip helper-address 192.168.30.10    ! Même serveur DHCP
```

**Vérification :**
```cisco
Router# show running-config | section interface
! Vérifier que ip helper-address est bien sur les bonnes interfaces

Router# show ip helper-address           ! Sur certaines versions IOS
```

#### Côté Serveur DHCP Linux

Le serveur DHCP doit avoir **une étendue (subnet) pour chaque VLAN** qu'il sert :

```
# /etc/dhcp/dhcpd.conf

# Étendue pour le VLAN 10 (réseau Informatique)
subnet 192.168.10.0 netmask 255.255.255.0 {
    range 192.168.10.100 192.168.10.200;
    option routers 192.168.10.1;              # IP du routeur côté VLAN 10
    option domain-name-servers 192.168.30.10; # IP du serveur DNS
    default-lease-time 86400;
}

# Étendue pour le VLAN 20 (réseau Direction)
subnet 192.168.20.0 netmask 255.255.255.0 {
    range 192.168.20.100 192.168.20.200;
    option routers 192.168.20.1;              # IP du routeur côté VLAN 20
    option domain-name-servers 192.168.30.10;
    default-lease-time 86400;
}

# Déclaration du réseau serveur (obligatoire même si pas de range DHCP ici)
# Bind9 doit connaître ce réseau pour accepter les requêtes relayées
subnet 192.168.30.0 netmask 255.255.255.0 {
    # Pas de range : les serveurs ont des IPs fixes
}
```

> **Important :** Le serveur DHCP Linux doit avoir une déclaration `subnet` pour **tous les réseaux** qu'il connaît, même ceux sans plage d'attribution. Sinon, il refuse de traiter les requêtes relayées.

---

### D. Schéma Complet de l'Infrastructure

```
┌─────────────────────────────────────────────────────────────────────┐
│                    INFRASTRUCTURE COMPLÈTE                          │
│                                                                     │
│  ┌──────────────┐    trunk    ┌──────────────────────────────────┐  │
│  │   SW_ACCÈS   │────────────│  ROUTEUR R1                      │  │
│  │  (Cisco 2960)│            │                                  │  │
│  │              │            │  Fa0/0.10  192.168.10.1/24       │  │
│  │ VLAN 10:     │            │    ip helper-address 192.168.30.10│  │
│  │  Fa0/1-Fa0/10│            │                                  │  │
│  │ VLAN 20:     │            │  Fa0/0.20  192.168.20.1/24       │  │
│  │  Fa0/11-Fa0/20│           │    ip helper-address 192.168.30.10│  │
│  │ Trunk:       │            │                                  │  │
│  │  Fa0/24 ─────┤            │  Fa0/0.30  192.168.30.1/24       │  │
│  └──────────────┘            └──────────┬───────────────────────┘  │
│                                         │ Fa0/0                     │
│  ┌──────────────────────────────────────┘                           │
│  │                                                                  │
│  │  ┌───────────────────────────────────────────────────────────┐  │
│  │  │  SERVEUR LINUX (VM Debian) – 192.168.30.10                │  │
│  │  │  ┌─────────────────────┐  ┌──────────────────────────────┐│  │
│  │  │  │  isc-dhcp-server    │  │  Bind9 (DNS)                 ││  │
│  │  │  │  2 étendues :       │  │  Zone : infra.local          ││  │
│  │  │  │  ▸ 192.168.10.0/24  │  │  A, PTR, CNAME               ││  │
│  │  │  │  ▸ 192.168.20.0/24  │  │                              ││  │
│  │  │  └─────────────────────┘  └──────────────────────────────┘│  │
│  │  └───────────────────────────────────────────────────────────┘  │
│  │   [VLAN 30 - Serveurs]                                           │
│  │   SW_ACCÈS Fa0/24 → R1 → Fa0/1 (accès) → Srv Linux             │
│  └──────────────────────────────────────────────────────────────────│
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ TP INTÉGRÉ GUIDÉ
### "Infrastructure Complète 2 VLANs + Routage + DHCP + DNS"

> **Note :** Ce TP est la **répétition générale** avant l'évaluation. L'enseignant peut guider et répondre aux questions. Les apprenants peuvent s'entraider. Durée : 60 minutes.

---

### Plan d'Adressage du TP Intégré

| **Réseau** | **VLAN** | **Adresse réseau** | **Passerelle** | **Plage DHCP** |
|------------|----------|-------------------|----------------|----------------|
| Informatique | VLAN 10 | 192.168.10.0/24 | 192.168.10.1 | .100 → .200 |
| Direction | VLAN 20 | 192.168.20.0/24 | 192.168.20.1 | .100 → .180 |
| Serveurs | VLAN 30 | 192.168.30.0/24 | 192.168.30.1 | (pas de DHCP) |
| Serveur Linux | – | 192.168.30.10 | 192.168.30.1 | IP fixe |

**Noms DNS (`infra.local`) :**
| **Nom** | **IP** | **Type** |
|---------|--------|---------|
| ns1.infra.local | 192.168.30.10 | A |
| srv-dhcp.infra.local | 192.168.30.10 | A (alias) |
| gw.infra.local | 192.168.30.1 | A |
| intranet.infra.local | 192.168.10.50 | A |

---

### Partie A – Configuration du Switch (Packet Tracer, ~15 min)

```cisco
! Accès CLI du switch
Switch> enable
Switch# configure terminal
Switch(config)# hostname SW_ACCÈS

! Créer les VLANs
SW_ACCÈS(config)# vlan 10
SW_ACCÈS(config-vlan)# name Informatique
SW_ACCÈS(config-vlan)# exit

SW_ACCÈS(config)# vlan 20
SW_ACCÈS(config-vlan)# name Direction
SW_ACCÈS(config-vlan)# exit

SW_ACCÈS(config)# vlan 30
SW_ACCÈS(config-vlan)# name Serveurs
SW_ACCÈS(config-vlan)# exit

! Affecter les ports aux VLANs (mode access)
SW_ACCÈS(config)# interface range FastEthernet0/1 - 5
SW_ACCÈS(config-if-range)# switchport mode access
SW_ACCÈS(config-if-range)# switchport access vlan 10
SW_ACCÈS(config-if-range)# exit

SW_ACCÈS(config)# interface range FastEthernet0/6 - 10
SW_ACCÈS(config-if-range)# switchport mode access
SW_ACCÈS(config-if-range)# switchport access vlan 20
SW_ACCÈS(config-if-range)# exit

SW_ACCÈS(config)# interface range FastEthernet0/11 - 15
SW_ACCÈS(config-if-range)# switchport mode access
SW_ACCÈS(config-if-range)# switchport access vlan 30
SW_ACCÈS(config-if-range)# exit

! Configurer le port trunk vers le routeur
SW_ACCÈS(config)# interface FastEthernet0/24
SW_ACCÈS(config-if)# switchport mode trunk
SW_ACCÈS(config-if)# switchport trunk allowed vlan 10,20,30
SW_ACCÈS(config-if)# exit

! Sauvegarder
SW_ACCÈS(config)# end
SW_ACCÈS# copy running-config startup-config

! Vérifier
SW_ACCÈS# show vlan brief
SW_ACCÈS# show interfaces trunk
```

✅ **Validation Étape A :** `show vlan brief` montre les 3 VLANs avec leurs ports. `show interfaces trunk` montre Fa0/24 en trunk avec les 3 VLANs autorisés.

---

### Partie B – Configuration du Routeur (Packet Tracer, ~15 min)

```cisco
Router> enable
Router# configure terminal
Router(config)# hostname R1_INFRA
R1_INFRA(config)# no ip domain-lookup

! Activer l'interface physique (sans IP)
R1_INFRA(config)# interface FastEthernet0/0
R1_INFRA(config-if)# no shutdown
R1_INFRA(config-if)# exit

! Sous-interface VLAN 10 (Informatique)
R1_INFRA(config)# interface FastEthernet0/0.10
R1_INFRA(config-subif)# encapsulation dot1Q 10
R1_INFRA(config-subif)# ip address 192.168.10.1 255.255.255.0
R1_INFRA(config-subif)# ip helper-address 192.168.30.10   ! Agent relais DHCP
R1_INFRA(config-subif)# exit

! Sous-interface VLAN 20 (Direction)
R1_INFRA(config)# interface FastEthernet0/0.20
R1_INFRA(config-subif)# encapsulation dot1Q 20
R1_INFRA(config-subif)# ip address 192.168.20.1 255.255.255.0
R1_INFRA(config-subif)# ip helper-address 192.168.30.10   ! Agent relais DHCP
R1_INFRA(config-subif)# exit

! Sous-interface VLAN 30 (Serveurs)
R1_INFRA(config)# interface FastEthernet0/0.30
R1_INFRA(config-subif)# encapsulation dot1Q 30
R1_INFRA(config-subif)# ip address 192.168.30.1 255.255.255.0
R1_INFRA(config-subif)# exit

! Sécurité de base
R1_INFRA(config)# enable secret Infra@2024
R1_INFRA(config)# banner motd #INFRA - Acces restreint#

R1_INFRA(config)# end
R1_INFRA# copy running-config startup-config

! Vérifications
R1_INFRA# show ip interface brief
R1_INFRA# show running-config | section interface
```

✅ **Validation Étape B :** `show ip interface brief` montre Fa0/0.10, Fa0/0.20, Fa0/0.30 en **up/up** avec leurs IPs. `show running-config` montre `ip helper-address` sur .10 et .20.

---

### Partie C – Configuration du Serveur DHCP Linux (VM Debian, ~15 min)

**Configurer l'IP fixe du serveur Linux (si pas déjà fait) :**

```bash
# Vérifier l'interface connectée au réseau VLAN 30
ip addr show

# Configurer une IP fixe si nécessaire (selon l'interface)
sudo nano /etc/network/interfaces
```

```
# /etc/network/interfaces
auto lo
iface lo inet loopback

auto eth0
iface eth0 inet static
    address 192.168.30.10
    netmask 255.255.255.0
    gateway 192.168.30.1
    dns-nameservers 127.0.0.1 8.8.8.8
```

```bash
sudo systemctl restart networking
ip addr show eth0   # Vérifier : 192.168.30.10/24
```

**Configurer dhcpd.conf :**

```bash
sudo nano /etc/dhcp/dhcpd.conf
```

```
option domain-name "infra.local";
option domain-name-servers 192.168.30.10;
default-lease-time 3600;
max-lease-time 86400;
authoritative;

# Réseau VLAN 10 - Informatique
subnet 192.168.10.0 netmask 255.255.255.0 {
    range 192.168.10.100 192.168.10.200;
    option routers 192.168.10.1;
    option domain-name-servers 192.168.30.10;
}

# Réseau VLAN 20 - Direction
subnet 192.168.20.0 netmask 255.255.255.0 {
    range 192.168.20.100 192.168.20.180;
    option routers 192.168.20.1;
    option domain-name-servers 192.168.30.10;
}

# Réseau VLAN 30 - Serveurs (déclaré mais sans plage)
subnet 192.168.30.0 netmask 255.255.255.0 {
    # Pas de range : IP fixes uniquement
}
```

```bash
# Vérifier la syntaxe
sudo dhcpd -t -cf /etc/dhcp/dhcpd.conf

# Démarrer/redémarrer
sudo systemctl restart isc-dhcp-server
sudo systemctl status isc-dhcp-server   # Doit être active (running)
```

✅ **Validation Étape C :** `systemctl status isc-dhcp-server` : active. 3 subnets déclarés dans dhcpd.conf.

---

### Partie D – Configuration du Serveur DNS Linux (VM Debian, ~10 min)

```bash
sudo nano /etc/bind/named.conf.local
```

Ajouter :
```
zone "infra.local" {
    type master;
    file "/etc/bind/db.infra.local";
};

zone "30.168.192.in-addr.arpa" {
    type master;
    file "/etc/bind/db.192.168.30.rev";
};
```

```bash
sudo nano /etc/bind/db.infra.local
```

```
$TTL 3600
@   IN  SOA ns1.infra.local. admin.infra.local. (
                2024112001 3600 900 604800 3600 )

@           IN  NS  ns1.infra.local.

ns1         IN  A   192.168.30.10
srv-dhcp    IN  A   192.168.30.10
gw          IN  A   192.168.30.1
intranet    IN  A   192.168.10.50
serveur     IN  CNAME   ns1
```

```bash
sudo nano /etc/bind/db.192.168.30.rev
```

```
$TTL 3600
@   IN  SOA ns1.infra.local. admin.infra.local. (
                2024112001 3600 900 604800 3600 )
@   IN  NS  ns1.infra.local.

1   IN  PTR gw.infra.local.
10  IN  PTR ns1.infra.local.
```

```bash
sudo named-checkzone infra.local /etc/bind/db.infra.local
sudo systemctl restart bind9
dig @127.0.0.1 ns1.infra.local         # Test résolution directe
dig @127.0.0.1 -x 192.168.30.10        # Test résolution inverse
```

✅ **Validation Étape D :** Les deux `dig` retournent les bonnes réponses avec le flag `aa`.

---

### Partie E – Tests Finaux (5 min)

**Depuis un PC Cisco dans Packet Tracer (VLAN 10, configuré en DHCP) :**

1. Le PC doit obtenir une IP dans la plage `.100-.200`
2. La passerelle doit être `192.168.10.1`
3. Le DNS doit être `192.168.30.10`

```
PC1> ipconfig /all           # Vérifier IP, masque, GW, DNS
PC1> ping 192.168.20.101     # Ping un PC VLAN 20 (inter-VLAN)
PC1> ping 192.168.30.10      # Ping le serveur Linux
```

**Tableau de validation TP intégré :**

| **Test** | **Commande** | **Résultat attendu** | **✅/❌** |
|----------|--------------|----------------------|----------|
| PC VLAN10 obtient IP DHCP | `ipconfig` | IP dans .10.100-.200 | |
| PC VLAN20 obtient IP DHCP | `ipconfig` | IP dans .20.100-.180 | |
| Ping inter-VLAN | PC VLAN10 → ping .20.101 | 4/4 réponses | |
| Ping vers serveur | PC VLAN10 → ping 192.168.30.10 | 4/4 réponses | |
| DNS résout A | `dig @192.168.30.10 ns1.infra.local` | 192.168.30.10 | |
| Baux DHCP actifs | `cat /var/lib/dhcp/dhcpd.leases` | Entrées actives | |

---

## 📋 BILAN PHASE 2 : ÉVALUATION FORMATIVE (TP NOTÉ)

---

### ⚠️ SUJET DE TP NOTÉ – À DISTRIBUER À LA PAUSE

---

# SUJET D'ÉVALUATION FORMATIVE – PHASE 2
## "Infrastructure Réseau Complète : ConsultCorp SARL"

**Durée :** 2 heures | **Documents :** Fiche aide-mémoire fournie + notes personnelles | **Travail individuel**

---

### Contexte Professionnel

Vous êtes **technicien réseau** chez **NetWork Solutions**. Votre client, **ConsultCorp SARL** (cabinet de conseil, 25 personnes), vous demande de déployer son infrastructure réseau depuis zéro.

Le client a fourni son plan réseau :

| **Département** | **VLAN** | **Réseau** | **Nb de postes** |
|-----------------|----------|------------|-----------------|
| Commercial | VLAN 10 | 192.168.100.0/24 | 10 postes |
| RH | VLAN 20 | 192.168.200.0/24 | 8 postes |
| Serveurs | VLAN 99 | 10.10.10.0/24 | IP fixes uniquement |

**Équipements à configurer :**
- 1 switch Cisco 2960 (`SW-CONSULTCORP`)
- 1 routeur Cisco (`R1-CONSULTCORP`)
- 1 serveur Linux Debian (`SRV-LINUX`, IP : `10.10.10.10/24`, passerelle : `10.10.10.1`)

**Noms DNS à créer (domaine : `consultcorp.local`) :**

| **Nom** | **IP** | **Remarque** |
|---------|--------|--------------|
| ns1.consultcorp.local | 10.10.10.10 | Serveur DNS |
| srv.consultcorp.local | 10.10.10.10 | Enregistrement supplémentaire |
| gw.consultcorp.local | 10.10.10.1 | Passerelle |
| portail.consultcorp.local | – | Alias CNAME vers ns1 |

**Convention mot de passe ConsultCorp (à respecter) :**
- `enable secret` : `ConsultCorp@2024`
- Console/VTY : `Console@CC`, `Vty@CC`

---

### Section 1 – Switch Cisco (6 points)

**Topologie switch (Packet Tracer) :**

```
SW-CONSULTCORP
├── Fa0/1 à Fa0/5  → VLAN 10 (Commercial)
├── Fa0/6 à Fa0/10 → VLAN 20 (RH)
├── Fa0/11 à Fa0/15 → VLAN 99 (Serveurs)
└── Fa0/24          → TRUNK vers R1-CONSULTCORP
```

**À réaliser :**

**1.1** (1 pt) — Créer les 3 VLANs avec les noms `Commercial`, `RH`, `Serveurs`.

**1.2** (2 pts) — Affecter les ports aux VLANs en mode access (plages ci-dessus).

**1.3** (1 pt) — Configurer Fa0/24 en mode trunk avec les 3 VLANs autorisés.

**1.4** (1 pt) — Appliquer les conventions de sécurité (hostname, enable secret, console, VTY).

**1.5** (1 pt) — Sauvegarder la configuration et fournir la capture de `show vlan brief`.

---

### Section 2 – Routeur Cisco (8 points)

**Plan sous-interfaces routeur :**

| **Sous-interface** | **VLAN** | **IP** | **Agent relais** |
|--------------------|----------|--------|-----------------|
| Fa0/0.10 | VLAN 10 | 192.168.100.1/24 | 10.10.10.10 |
| Fa0/0.20 | VLAN 20 | 192.168.200.1/24 | 10.10.10.10 |
| Fa0/0.99 | VLAN 99 | 10.10.10.1/24 | (pas de relay) |

**À réaliser :**

**2.1** (1 pt) — Configurer le hostname `R1-CONSULTCORP` et désactiver la résolution DNS locale (`no ip domain-lookup`).

**2.2** (3 pts) — Configurer les 3 sous-interfaces avec `encapsulation dot1Q`, IP, et `no shutdown`. La sous-interface Fa0/0 physique doit aussi être activée.

**2.3** (2 pts) — Configurer l'agent relais DHCP (`ip helper-address 10.10.10.10`) sur les sous-interfaces des VLANs 10 et 20 **uniquement**.

**2.4** (1 pt) — Appliquer les conventions de sécurité (enable secret, banner, console, VTY).

**2.5** (1 pt) — Sauvegarder et fournir la capture de `show ip interface brief`.

---

### Section 3 – Serveur DHCP Linux (8 points)

Sur la **VM Debian** (`SRV-LINUX`), configurer le serveur isc-dhcp-server :

**3.1** (1 pt) — Vérifier que l'IP `10.10.10.10/24` est bien configurée sur l'interface réseau. Fournir la capture de `ip addr show`.

**3.2** (3 pts) — Configurer `/etc/dhcp/dhcpd.conf` avec :
- Options globales : `domain-name "consultcorp.local"`, `domain-name-servers 10.10.10.10`, bail par défaut 8h
- Une étendue pour le VLAN 10 (plage : `192.168.100.50` → `192.168.100.150`, passerelle `.100.1`)
- Une étendue pour le VLAN 20 (plage : `192.168.200.50` → `192.168.200.100`, passerelle `.200.1`)
- Une déclaration subnet pour `10.10.10.0/24` (sans range)

**3.3** (1 pt) — Déclarer l'interface d'écoute dans `/etc/default/isc-dhcp-server`.

**3.4** (1 pt) — Vérifier la syntaxe (`dhcpd -t`) et démarrer le service. Fournir la capture de `systemctl status isc-dhcp-server`.

**3.5** (2 pts) — Tester l'attribution DHCP depuis Packet Tracer (un PC en DHCP dans chaque VLAN) et fournir :
- 2 captures `ipconfig` dans Packet Tracer (1 par VLAN) montrant les IPs obtenues dans la bonne plage

> **Astuce** : Pour tester le DHCP inter-VLAN depuis Packet Tracer, configurez les PCs en DHCP (IP Configuration → DHCP) et attendez quelques secondes que la requête soit relayée.

---

### Section 4 – Serveur DNS Linux (6 points)

Toujours sur la **VM Debian**, configurer Bind9 pour le domaine `consultcorp.local` :

**4.1** (1 pt) — Déclarer les zones directe et inverse dans `/etc/bind/named.conf.local`.

**4.2** (2 pts) — Créer le fichier de zone directe `/etc/bind/db.consultcorp.local` avec :
- SOA correct (serial : `2024112001`)
- 1 enregistrement NS
- 3 enregistrements A (`ns1`, `srv`, `gw`)
- 1 enregistrement CNAME (`portail` → `ns1`)

**4.3** (1 pt) — Créer le fichier de zone inverse `/etc/bind/db.10.10.10.rev` avec les PTR pour les IPs `.1` et `.10`.

**4.4** (1 pt) — Vérifier avec `named-checkzone` (les deux zones) et redémarrer Bind9. Fournir les captures des deux vérifications (OK).

**4.5** (1 pt) — Tester avec `dig` :
- `dig @127.0.0.1 ns1.consultcorp.local` → IP 10.10.10.10, flag `aa`
- `dig @127.0.0.1 portail.consultcorp.local` → CNAME + IP
- `dig @127.0.0.1 -x 10.10.10.10` → ns1.consultcorp.local.

Fournir les 3 captures `dig`.

---

### Section 5 – Rapport de Synthèse (2 points)

Rédiger un **court rapport technique** (15 à 20 lignes) destiné au directeur technique de ConsultCorp, expliquant :
- Ce qui a été configuré (en termes fonctionnels, pas techniques)
- Comment vérifier que le réseau fonctionne correctement (3 tests à faire en autonomie)
- 1 recommandation pour améliorer la fiabilité de l'infrastructure

---

### Barème de l'Évaluation

| **Section** | **Critère clé** | **Points** |
|-------------|-----------------|------------|
| **1 – Switch** | 3 VLANs + ports + trunk + sécu + save | **/6** |
| **2 – Routeur** | 3 sous-interfaces + relay sur 2 interfaces + sécu + save | **/8** |
| **3 – DHCP Linux** | 2 étendues + déclaration subnet serveurs + service actif + test IPs | **/8** |
| **4 – DNS Linux** | Zone directe + PTR + vérification + 3 tests dig réussis | **/6** |
| **5 – Rapport** | Clair, pertinent, recommandation justifiée | **/2** |
| **TOTAL** | | **/30** |

**Conversion /30 → /20 :**

| **/30** | **/20** | | **/30** | **/20** |
|---------|---------|--|---------|---------|
| 30 | 20 | | 15 | 10 |
| 27 | 18 | | 12 | 8 |
| 24 | 16 | | 9 | 6 |
| 21 | 14 | | 6 | 4 |
| 18 | 12 | | ≤3 | 2 |

> **Formule exacte :** Note/20 = (points obtenus / 30) × 20, arrondi au demi-point supérieur.

---

## ✅ CORRECTION COMPLÈTE DU TP NOTÉ

*(Réservée à l'enseignant)*

---

### Section 1 – Configuration Switch Complète

```cisco
Switch> enable
Switch# configure terminal
Switch(config)# hostname SW-CONSULTCORP

! VLANs
SW-CONSULTCORP(config)# vlan 10
SW-CONSULTCORP(config-vlan)# name Commercial
SW-CONSULTCORP(config-vlan)# exit
SW-CONSULTCORP(config)# vlan 20
SW-CONSULTCORP(config-vlan)# name RH
SW-CONSULTCORP(config-vlan)# exit
SW-CONSULTCORP(config)# vlan 99
SW-CONSULTCORP(config-vlan)# name Serveurs
SW-CONSULTCORP(config-vlan)# exit

! Ports access VLAN 10
SW-CONSULTCORP(config)# interface range FastEthernet0/1 - 5
SW-CONSULTCORP(config-if-range)# switchport mode access
SW-CONSULTCORP(config-if-range)# switchport access vlan 10
SW-CONSULTCORP(config-if-range)# exit

! Ports access VLAN 20
SW-CONSULTCORP(config)# interface range FastEthernet0/6 - 10
SW-CONSULTCORP(config-if-range)# switchport mode access
SW-CONSULTCORP(config-if-range)# switchport access vlan 20
SW-CONSULTCORP(config-if-range)# exit

! Ports access VLAN 99
SW-CONSULTCORP(config)# interface range FastEthernet0/11 - 15
SW-CONSULTCORP(config-if-range)# switchport mode access
SW-CONSULTCORP(config-if-range)# switchport access vlan 99
SW-CONSULTCORP(config-if-range)# exit

! Trunk
SW-CONSULTCORP(config)# interface FastEthernet0/24
SW-CONSULTCORP(config-if)# switchport mode trunk
SW-CONSULTCORP(config-if)# switchport trunk allowed vlan 10,20,99
SW-CONSULTCORP(config-if)# exit

! Sécurité
SW-CONSULTCORP(config)# enable secret ConsultCorp@2024
SW-CONSULTCORP(config)# banner motd #ConsultCorp SARL - Acces restreint#
SW-CONSULTCORP(config)# line console 0
SW-CONSULTCORP(config-line)# password Console@CC
SW-CONSULTCORP(config-line)# login
SW-CONSULTCORP(config-line)# exit
SW-CONSULTCORP(config)# line vty 0 15
SW-CONSULTCORP(config-line)# password Vty@CC
SW-CONSULTCORP(config-line)# login
SW-CONSULTCORP(config-line)# exit
SW-CONSULTCORP(config)# service password-encryption
SW-CONSULTCORP(config)# end
SW-CONSULTCORP# copy running-config startup-config
```

**Résultat `show vlan brief` attendu :**
```
VLAN Name                             Status    Ports
---- -------------------------------- --------- --------------------
1    default                          active    Fa0/16, Fa0/17...
10   Commercial                       active    Fa0/1, Fa0/2, Fa0/3, Fa0/4, Fa0/5
20   RH                               active    Fa0/6, Fa0/7, Fa0/8, Fa0/9, Fa0/10
99   Serveurs                         active    Fa0/11, Fa0/12, Fa0/13, Fa0/14, Fa0/15
```

---

### Section 2 – Configuration Routeur Complète

```cisco
Router> enable
Router# configure terminal
Router(config)# hostname R1-CONSULTCORP
R1-CONSULTCORP(config)# no ip domain-lookup

! Interface physique (pas d'IP, juste activation)
R1-CONSULTCORP(config)# interface FastEthernet0/0
R1-CONSULTCORP(config-if)# no shutdown
R1-CONSULTCORP(config-if)# exit

! Sous-interface VLAN 10 - Commercial
R1-CONSULTCORP(config)# interface FastEthernet0/0.10
R1-CONSULTCORP(config-subif)# encapsulation dot1Q 10
R1-CONSULTCORP(config-subif)# ip address 192.168.100.1 255.255.255.0
R1-CONSULTCORP(config-subif)# ip helper-address 10.10.10.10
R1-CONSULTCORP(config-subif)# exit

! Sous-interface VLAN 20 - RH
R1-CONSULTCORP(config)# interface FastEthernet0/0.20
R1-CONSULTCORP(config-subif)# encapsulation dot1Q 20
R1-CONSULTCORP(config-subif)# ip address 192.168.200.1 255.255.255.0
R1-CONSULTCORP(config-subif)# ip helper-address 10.10.10.10
R1-CONSULTCORP(config-subif)# exit

! Sous-interface VLAN 99 - Serveurs (pas de relay)
R1-CONSULTCORP(config)# interface FastEthernet0/0.99
R1-CONSULTCORP(config-subif)# encapsulation dot1Q 99
R1-CONSULTCORP(config-subif)# ip address 10.10.10.1 255.255.255.0
R1-CONSULTCORP(config-subif)# exit

! Sécurité
R1-CONSULTCORP(config)# enable secret ConsultCorp@2024
R1-CONSULTCORP(config)# banner motd #R1-CONSULTCORP - Acces restreint#
R1-CONSULTCORP(config)# line console 0
R1-CONSULTCORP(config-line)# password Console@CC
R1-CONSULTCORP(config-line)# login
R1-CONSULTCORP(config-line)# exit
R1-CONSULTCORP(config)# line vty 0 15
R1-CONSULTCORP(config-line)# password Vty@CC
R1-CONSULTCORP(config-line)# login
R1-CONSULTCORP(config-line)# exit
R1-CONSULTCORP(config)# service password-encryption
R1-CONSULTCORP(config)# end
R1-CONSULTCORP# copy running-config startup-config
```

**Résultat `show ip interface brief` attendu :**
```
Interface              IP-Address      OK? Method Status      Protocol
FastEthernet0/0        unassigned      YES unset  up          up
FastEthernet0/0.10     192.168.100.1   YES manual up          up
FastEthernet0/0.20     192.168.200.1   YES manual up          up
FastEthernet0/0.99     10.10.10.1      YES manual up          up
```

---

### Section 3 – dhcpd.conf Complet

```
option domain-name "consultcorp.local";
option domain-name-servers 10.10.10.10;
default-lease-time 28800;     # 8 heures
max-lease-time 86400;
authoritative;

# VLAN 10 - Commercial
subnet 192.168.100.0 netmask 255.255.255.0 {
    range 192.168.100.50 192.168.100.150;
    option routers 192.168.100.1;
    option domain-name-servers 10.10.10.10;
}

# VLAN 20 - RH
subnet 192.168.200.0 netmask 255.255.255.0 {
    range 192.168.200.50 192.168.200.100;
    option routers 192.168.200.1;
    option domain-name-servers 10.10.10.10;
}

# VLAN 99 - Serveurs (sans range DHCP)
subnet 10.10.10.0 netmask 255.255.255.0 {
    # Pas d'attribution DHCP ici
}
```

---

### Section 4 – Fichiers de Zone Bind9

**db.consultcorp.local :**
```
$TTL 3600
@   IN  SOA ns1.consultcorp.local. admin.consultcorp.local. (
                2024112001 3600 900 604800 3600 )

@           IN  NS  ns1.consultcorp.local.

ns1         IN  A   10.10.10.10
srv         IN  A   10.10.10.10
gw          IN  A   10.10.10.1

portail     IN  CNAME   ns1
```

**db.10.10.10.rev :**
```
$TTL 3600
@   IN  SOA ns1.consultcorp.local. admin.consultcorp.local. (
                2024112001 3600 900 604800 3600 )

@   IN  NS  ns1.consultcorp.local.

1   IN  PTR gw.consultcorp.local.
10  IN  PTR ns1.consultcorp.local.
```

**named.conf.local :**
```
zone "consultcorp.local" {
    type master;
    file "/etc/bind/db.consultcorp.local";
};

zone "10.10.10.in-addr.arpa" {
    type master;
    file "/etc/bind/db.10.10.10.rev";
};
```

---

### Section 5 – Rapport de Synthèse (exemple)

> *"Voici ce qui a été mis en place pour ConsultCorp SARL :*
>
> *Le réseau est maintenant divisé en 3 zones séparées : une pour le service Commercial, une pour les Ressources Humaines, et une pour les serveurs. Cette séparation garantit que le trafic de chaque département reste isolé, ce qui améliore la sécurité.*
>
> *Chaque poste de travail obtient automatiquement son adresse réseau au démarrage (service DHCP), sans aucune manipulation de votre part. Le serveur mémorise également un annuaire interne (DNS) qui permet d'accéder aux serveurs par leur nom (`portail.consultcorp.local`) plutôt que par leur adresse numérique.*
>
> *Pour vérifier que tout fonctionne correctement : (1) Débrancher et rebrancher un PC et vérifier qu'il obtient une IP automatiquement. (2) Taper `ping gw.consultcorp.local` dans une invite de commande — vous devez avoir une réponse. (3) Taper `ping 192.168.200.50` depuis un poste Commercial — vous devez joindre le réseau RH.*
>
> *Recommandation : Installer un deuxième serveur DHCP/DNS de secours (redondance) pour garantir la continuité de service si le serveur principal tombe en panne."*

---

### Grille de Correction Détaillée (Enseignant)

| **Section** | **Critère** | **0 pt** | **Partiel** | **Plein** |
|-------------|-------------|----------|-------------|-----------|
| Switch 1.1 | 3 VLANs créés avec noms | Aucun | 1-2 VLANs | 3 VLANs corrects |
| Switch 1.2 | Ports affectés correctement | Mauvais VLAN | 1-2 plages | Les 3 plages correctes |
| Switch 1.3 | Trunk configuré | Non fait | Trunk sans VLAN explicite | Trunk + 3 VLANs autorisés |
| Switch 1.4 | Sécurité complète | Aucune | Enable secret seulement | Enable + console + VTY + banner |
| Routeur 2.2 | 3 sous-interfaces up/up | <2 up | 2 sur 3 | 3/3 up, IPs correctes |
| Routeur 2.3 | helper-address sur .10 et .20 | Non configuré | 1 seul | 2 sous-interfaces correctement |
| DHCP 3.2 | 2 étendues + subnet serveurs | <2 subnets | 2 sans subnet serveur | 3 subnets, plages correctes |
| DHCP 3.4 | Service actif, pas d'erreur | Erreur syntax | Service démarre avec warning | Active (running) |
| DHCP 3.5 | IPs obtenues dans bonnes plages | Échec DHCP | 1 seul VLAN | VLAN 10 et 20 testés, bonnes IPs |
| DNS 4.2 | Fichier zone directe complet | Absent | SOA+NS+A seulement | SOA+NS+3A+CNAME corrects |
| DNS 4.3 | Zone inverse avec PTR | Absent | 1 PTR | .1 et .10 corrects, point final |
| DNS 4.4 | checkzone OK + bind9 redémarré | Erreur syntaxe | 1 zone OK | 2 zones OK + service running |
| DNS 4.5 | 3 `dig` réussis avec flag aa | <2 réussites | 2 sur 3 | 3/3, flag aa présent |
| Rapport 5 | Clarté + 3 tests + recommandation | Absent | Techniques seulement | Accessible, complet |

---

### Synthèse Bilan Phase 2 – Points Clés à Valider

À l'issue de la S10, un apprenant maîtrisant la Phase 2 doit être capable de :

**Sans aide :**
- Lire et analyser une topologie réseau avec VLANs
- Configurer un switch (VLANs, ports access, trunk) en autonomie
- Configurer un routeur pour le routage inter-VLAN (router-on-a-stick)
- Expliquer et configurer l'agent relais DHCP
- Écrire un fichier dhcpd.conf complet avec 2 étendues
- Créer un fichier de zone DNS directe avec SOA, NS, A, CNAME
- Tester avec dig, ipconfig, ping et interpréter les résultats

**Avec la fiche aide-mémoire :**
- Créer un fichier de zone inverse avec PTR
- Utiliser `named-checkzone` et corriger les erreurs de syntaxe
- Configurer les lignes VTY et consolider la sécurité Cisco

---

*Fin du Pack Semaine 10 – Bloc 2 : Agent Relais DHCP, TP Intégré et Bilan Phase 2*
